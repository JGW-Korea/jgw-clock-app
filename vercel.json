{
  "installCommand": "corepack enable && yarn install --immutable",
  "buildCommand": "yarn build",
  "devCommand": "yarn start",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}